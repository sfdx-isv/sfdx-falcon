

export enum INTENT {
  BUILD_SCRATCH_ORG,
  DEPLOY_DEMO,
  HEALTH_CHECK,
  NOT_SPECIFIED,
  REPAIR_PROJECT,
  VALIDATE_DEMO
}

export enum ERROR_TYPE {
  CLI_ERROR,
  INTERNAL_ERROR,
  FALCON_ERROR,
  UNPARSED_ERROR
}